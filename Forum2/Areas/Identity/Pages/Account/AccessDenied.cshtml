@page
@model AccessDeniedModel
@{
    ViewData["Title"] = "Access denied";
}

<div class="alert alert-danger">
    <h4 class="alert-heading">@ViewData["Title"]</h4>
    <p class="pb-0 mb-0">You do not have access to this resource.</p>
</div>